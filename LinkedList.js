//import node and have functionality
function createLinkedList() {
  // append(value)
  //prepend(value)
  // size
  // head
  // tail
  //at(index)
  // pop
  // contains(value)
  //find(value)
  //toString
  //insertAt(value, index)
  //removeAt(index)
}
